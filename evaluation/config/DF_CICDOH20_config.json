{
    "beta_length": 100,
    "beta_time_ms": 40,
    "pr_sel": 0.15,

    "sigma_vrs": 80,

    "lambda_shape": 0.001,
    "feature_noise_distribution": "gaussian",

    "optimizer": "adamw",
    "scheduler": "linear",
    "batch_size": 64,
    "epochs_num": 200,
    "early_stop": 50,
    "warmup": 0.01,
    "learning_rate": 1e-3,
    "weight_decay": 0.01
}